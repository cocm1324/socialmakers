export enum ROUTER_TYPE {
    DEFAULT = "",
    ACADEMY = "academy",
    LOGIN = "login"
}

export enum LOCAL_STORAGE_TYPE {
    TOKEN = "userJwt",
    LOGIN = "userId",
    CALLBACK = "callbackUrl"
}